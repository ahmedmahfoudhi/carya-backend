import { Prop, Schema } from "@nestjs/mongoose";
import {now} from "mongoose";

// @Schema()
// export class timeStamp {
//     @Prop({default: now()})
//     createdAt: Date;

//     @Prop({default: now()})
//     updatedAt: Date;
// }